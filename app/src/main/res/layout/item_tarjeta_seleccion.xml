<?xml version="1.0" encoding="utf-8"?>

<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="4dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="3dp"
    app:cardBackgroundColor="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="14dp"
        android:gravity="center_vertical">

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:scaleType="centerCrop"
            android:src="@drawable/icono_tarjetapred"
            android:layout_marginEnd="12dp" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/txtNombreTarjetaSel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Tarjeta 1"
                android:textStyle="bold"
                android:textSize="13sp"
                android:textColor="@color/black" />

            <TextView
                android:id="@+id/txtNumeroTarjetaSel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="522 ****** ***** 993"
                android:textSize="12sp"
                android:textColor="@color/gray_medium"
                android:layout_marginTop="2dp" />

        </LinearLayout>

        <!-- Estrella: el color se pone desde TarjetaSeleccionAdapter con setColorFilter()
             Amarilla (#FFC107) = predeterminada | gris (@color/gray_medium) = no seleccionada -->
        <ImageButton
            android:id="@+id/btnEstrellaTarjeta"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:backgroundTint="@color/transparent"
            android:src="@android:drawable/star_big_off"
            android:scaleType="centerCrop"
            android:contentDescription="Marcar como predeterminada" />

    </LinearLayout>

</androidx.cardview.widget.CardView>